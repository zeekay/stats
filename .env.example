# GitHub API Configuration
# Create a personal access token at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public only)
GITHUB_TOKEN=ghp_your_token_here

# Application Settings
DEBUG=True
PORT=5001

# Start date for analysis (YYYY-MM-DD)
# How far back to fetch commits from
START_DATE=2021-01-01

# Users to analyze (comma separated)
# Supports multiple accounts if you use several
GITHUB_USERS=your_username

# Cache directory (optional, defaults to ./cache)
# CACHE_DIR=./cache

# Request delay in seconds (optional, defaults to 0.1)
# Increase if you hit rate limits frequently
# REQUEST_DELAY=0.1
